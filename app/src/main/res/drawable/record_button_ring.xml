<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Transparent center -->
    <solid android:color="@android:color/transparent" />

    <!-- Visible white ring border -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" />

</shape>
